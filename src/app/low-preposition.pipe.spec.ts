import { LowPrepositionPipe } from './low-preposition.pipe';

describe('LowPrepositionPipe', () => {
  it('create an instance', () => {
    const pipe = new LowPrepositionPipe();
    expect(pipe).toBeTruthy();
  });
});
